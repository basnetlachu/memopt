{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "memopt.serviceAccountName" . }}
  labels:
    {{- include "memopt.labels" . | nindent 4 }}
{{- end }}
