# MemOpt Requirements
# GPU Memory Bandwidth Profiling & Optimization Platform

# Core dependencies (minimal install)
torch>=2.0.0
numpy>=1.24.0

# Optional: For LLM profiling examples
transformers>=4.30.0
accelerate>=0.20.0

# Optional: Development tools
pytest>=7.4.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0

# Optional: Enhanced visualization
matplotlib>=3.7.0
plotly>=5.14.0

# API server
fastapi>=0.100.0
uvicorn>=0.23.0
prometheus-client>=0.19.0
nvidia-ml-py>=12.0.0
psutil>=5.9.0

# Optional: GPUDirect Storage — NVMe→HBM direct DMA (~14 GB/s). Requires CUDA 11.4+, Linux, compatible NVMe.
# nvidia-gds
