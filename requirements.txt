# memopt runtime dependencies
# Pin minor versions — allows security patches, prevents breaking changes

# Core
torch>=2.0.0
psutil>=5.9.0

# Serving
fastapi>=0.100.0
uvicorn>=0.23.0

# Observability
prometheus-client>=0.17.0

# Cluster
redis>=5.0.0

# Kernel synthesis
anthropic>=0.20.0

# Daemon / GPU monitoring
pynvml>=11.0.0
pyyaml>=6.0

# HTTP client (control plane registration)
httpx>=0.24.0

# Database (PostgreSQL optional — SQLite default)
psycopg2-binary>=2.9.0

# ScyllaDB (optional — falls back to Redis or local)
cassandra-driver>=3.28.0
